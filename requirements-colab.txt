# Colab-only overrides. Used by:
#   - notebooks/phase05_finetune_retriever.ipynb (T4 GPU)
#   - notebooks/phase07_verifier_sweep.ipynb (T4/A100, optional)
#
# Colab runtimes already ship with torch+cuda; we override only the libs that
# need to match. Heavy GPU-specific libs (vLLM) are added per-notebook with
# !pip install at the top to avoid resolver conflicts on import.
#
# IMPORTANT: do NOT pin `typer` or `pydantic` here. Colab preinstalls
# `typer-slim>=0.24` and `pydantic>=2.11` (via google-adk / mcp), and pinning
# us to older versions triggers a TypeError on boolean CLI flags during
# Phase 05 training. The inline training cell in the notebook bypasses typer
# entirely; the only thing we strictly need is sentence-transformers.

sentence-transformers==3.4.1
loguru==0.7.2
